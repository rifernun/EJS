const resultado = nota => nota >= 7 ? 'aprovado' : 'reprovado'
// primeiro verifica se nota é maior ou igual a 7, se sim, aprovado, se nao, reprovado
/*
    if (nota >= 7) {
        console.log("aprovado")
    } else {
        console.log("reprovado")
    }

    tudo em uma linha!
*/